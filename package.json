{
  "private": true,
  "license": "Apache-2.0",
  "author": "Kinvey, Inc.",
  "scripts": {
    "build": "lerna run build --parallel",
    "publish": "lerna publish --skip-npm --preid=beta",
    "test": "lerna run test --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^2.5.1"
  }
}
