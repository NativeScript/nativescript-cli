{
  "private": true,
  "name": "kinvey",
  "scripts": {
    "build": "lerna run build",
    "prebundle": "npm run build",
    "bundle": "lerna run bundle",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.6.0"
  }
}
