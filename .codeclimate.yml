engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    exclude_paths:
    - "dist/"
    - "node_modules/"
    - "test/"
    checks:
      import/no-unresolved:
        enabled: false
ratings:
  paths:
  - src/**
